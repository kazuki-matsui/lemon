!import['lime.lemon', 'randomFunction.lemon']()
number = 5
string = 'hello'
boolean = true
nullValue = null
binaryExpression = (number * 5) ^ 5
array = ["red", "blue"]
object = {
    definition = 'objects act the same way as the upper level'
}
object.definition -> 'this manipulates the variable'
!constant = 'this variable cannot be changed'
array[0] -> "would change first index of array"
check(string = 'hello') {
    boolean = true
} otherwise(string = 'Hello') {
    boolean = false
}
intValue = 0
repeat(number) {
    intValue -> intValue + 1
    check(intValue > 100 | intValue = 100) {
        break
    }
}
intValue -> 0
while(number * math.randomDecimal[3, [1, 3]]() < 10) {
    intValue -> intValue * random[1, 10]()
}
intValue -> 0
repeat(index : array) {
    intValue -> index
}
function smartFunctionName[smartValue](smartArray) = {
    intValue -> 0
    smartArray = []
    repeat(math.random[null, smartValue]()) {
        smartArray[intValue] -> intValue + 1
        intValue -> intValue + 1
    }
}
smartFunctionName[5]()
/* this is a constant function, it's purpose is only to be useful to the devs*/
!summonMonster['troll']()